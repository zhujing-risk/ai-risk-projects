# 风控建模面试速记卡片（10张）
> 基于你的实际工作代码，覆盖面试最高频考点

---

## 卡片1：为什么用IV筛选变量？阈值为什么是0.02？

**速答：**
IV（Information Value）衡量变量对好坏客户的区分能力。

| IV范围 | 含义 |
|--------|------|
| < 0.02 | 几乎没有预测力，直接删 |
| 0.02 - 0.1 | 弱预测力 |
| 0.1 - 0.3 | 中等预测力（核心变量） |
| 0.3 - 0.5 | 强预测力（注意是否过拟合） |
| > 0.5 | 过强，可能有信息泄漏 |

**底层逻辑：**
IV = Σ (好客户占比 - 坏客户占比) × WOE

0.02是行业经验值。低于这个值说明该变量对区分好坏客户几乎没贡献，留着只会增加噪声。

**追问防守：** "IV>0.5为什么要警惕？"
→ 可能是因为该变量和目标变量有因果倒置（比如"是否被催收"预测"是否逾期"，本末倒置）

---

## 卡片2：WOE是什么？为什么要做WOE转换？

**速答：**
WOE（Weight of Evidence）= ln(该箱好客户占比 / 该箱坏客户占比)

**为什么要转换：**
1. 把分类变量和连续变量统一成数值
2. WOE天然和违约率呈线性关系 → 完美适配逻辑回归
3. 自动处理了异常值和非线性问题
4. 模型系数直接可解释

**你代码中怎么用的：**
```
原始变量 → OptimalBinning分箱 → 计算每箱WOE → 替换原始值 → 输入LR
```

**追问防守：** "WOE全是负数正常吗？"
→ 正常。WOE>0说明该箱好客户多，WOE<0说明坏客户多。符号方向取决于好坏比。

---

## 卡片3：为什么用LightGBM筛特征，却用LR建最终模型？

**速答：**
各取所长——LGBM擅长找重要变量，LR擅长给监管交代。

| 环节 | 用什么 | 为什么 |
|------|--------|--------|
| 特征筛选 | LightGBM | 能自动捕捉非线性、交互效应，找出真正有用的变量 |
| 最终评分卡 | Logistic Regression | 可解释、单调、监管认可、线上部署简单 |

**底层逻辑：**
- 银保监会/央行要求评分卡模型可解释（"为什么拒绝这个客户？"）
- LR的系数可以直接转化为评分 → "信用分=基础分+各变量得分"
- LGBM是黑盒，无法向监管解释单个客户的拒绝原因

**追问防守：** "为什么不直接用相关性筛特征？"
→ 相关性只能发现线性关系，会漏掉非线性重要特征。LGBM的feature_importance基于分裂增益，更全面。

---

## 卡片4：OptimalBinning是什么？为什么不等距/等频分箱？

**速答：**
OptimalBinning = 用算法（决策树/卡方检验）自动找最优切分点，使每箱内好坏比差异最大。

| 分箱方法 | 优点 | 缺点 |
|----------|------|------|
| 等距分箱 | 简单 | 可能把好坏混在同一箱 |
| 等频分箱 | 每箱样本均匀 | 不考虑目标变量 |
| **OptimalBinning** | 最大化IV/信息增益 | 需要调参，可能过拟合 |

**你代码中怎么用的：**
```python
from optbinning import OptimalBinning
optb = OptimalBinning(name=col, dtype="numerical", solver="cp")
optb.fit(X, y)
# solver="cp" → 约束规划，保证分箱结果单调
```

**追问防守：** "分几箱合适？"
→ 一般5-8箱。太少区分度不够，太多容易过拟合且线上维护复杂。

---

## 卡片5：PSI是什么？为什么阈值是0.25？

**速答：**
PSI（Population Stability Index）衡量模型评分分布是否发生漂移。

```
PSI = Σ (实际占比 - 预期占比) × ln(实际占比 / 预期占比)
```

| PSI范围 | 含义 | 行动 |
|---------|------|------|
| < 0.1 | 稳定 | 不用管 |
| 0.1 - 0.25 | 轻微漂移 | 关注 |
| > 0.25 | 显著漂移 | 触发模型重训 |

**底层逻辑：**
- 训练数据用的是2024年1-6月的客户
- 如果2025年客户的特征分布变了（比如疫情后收入结构变化），模型预测就不准了
- PSI就是用来监控"现在的客户还像不像训练时的客户"

**追问防守：** "PSI=0.3但KS还行，要重训吗？"
→ 要。PSI高意味着分布已偏移，当前KS可能是巧合，未来大概率恶化。

---

## 卡片6：单调性为什么重要？怎么检验？

**速答：**
单调性 = 评分越高，违约率越低（或越高），不能忽高忽低。

**为什么重要：**
1. 业务可解释性：收入越高→风险越低，这才合逻辑
2. 监管要求：评分卡必须"分数高=风险低"
3. 策略落地：如果不单调，就没法设cutoff（"550分以上通过"没意义了）

**你代码中的检验方法：**
```python
# Cochran-Armitage趋势检验 → 检验"分箱序号"和"违约率"是否有显著线性趋势
# p-value < 0.05 → 趋势显著 → 单调性成立

# Kendall Tau相关系数 → 检验排序一致性
# |tau| 接近1 → 强单调
```

**追问防守：** "如果某个变量不单调怎么办？"
→ 三种处理：①合并非单调的箱 ②U型关系拆成两个变量 ③直接剔除

---

## 卡片7：LightGBM关键参数怎么调？

**速答（你代码中的参数）：**

```python
params = {
    'n_estimators': 200,      # 树的数量，越多越容易过拟合
    'max_depth': 5,           # 每棵树最大深度，控制复杂度
    'learning_rate': 0.05,    # 学习率，越小越稳但需要更多树
    'num_leaves': 31,         # 叶子数，LGBM用leaf-wise生长
    'min_child_samples': 50,  # 叶子最少样本数，防过拟合
    'subsample': 0.8,         # 每棵树用80%样本，增加随机性
    'colsample_bytree': 0.8,  # 每棵树用80%特征
    'reg_alpha': 0.1,         # L1正则化
    'reg_lambda': 0.1,        # L2正则化
}
```

**面试高频追问：**
- "num_leaves和max_depth什么关系？" → num_leaves ≤ 2^max_depth，LGBM优先控制leaves
- "learning_rate调小了要怎么配合？" → n_estimators要相应调大
- "怎么防过拟合？" → early_stopping + 降max_depth + 加正则 + 降采样

---

## 卡片8：相关性过滤为什么要做？阈值多少？

**速答：**
高相关的变量（如"月收入"和"年收入"）提供的信息重复，保留一个即可。

**你代码中的逻辑：**
```python
# 1. 计算所有变量两两相关系数（Pearson）
# 2. 相关系数 > 0.7 的变量对，删掉IV较低的那个
# 3. 保留信息量更大的变量
```

**为什么阈值是0.7？**
- 0.7是经验值，r²=0.49，说明一个变量能解释另一个49%的方差
- 太严格（0.5）会删太多有用变量
- 太宽松（0.9）共线性问题仍然存在

**底层逻辑：**
LR对多重共线性敏感 → 共线性导致系数不稳定/符号翻转 → 评分卡不可解释

**追问防守：** "VIF和相关系数有什么区别？"
→ 相关系数看两两关系，VIF看一个变量被其他所有变量联合解释的程度。VIF>10说明有严重共线性。

---

## 卡片9：KS和AUC有什么区别？为什么都要看？

**速答：**

| 指标 | 含义 | 关注点 |
|------|------|--------|
| KS | 好坏客户累计分布最大分离度 | 最优切分点在哪 |
| AUC | 模型整体排序能力 | 全局表现 |

**类比理解：**
- AUC像"平均分"：整体排名好不好
- KS像"单科最高分"：最擅长区分的那个点有多强

**你代码中的标准：**
- KS=20%（行业一般15-35%算合格）
- AUC=60%（看起来低，但信贷领域60-75%很正常，不像Kaggle动辄0.99）

**追问防守：** "KS在哪个分位点最大？有什么用？"
→ KS最大的点就是cutoff参考点。比如KS在评分550最大，说明550是最佳区分好坏的阈值。

---

## 卡片10：你们代码的完整Pipeline顺序是什么？每步为什么这么排？

**速答（按你代码顺序）：**

```
1. ODPS取数              → 原始宽表
2. IV计算               → 粗筛，删无用变量（500→200）
3. OptimalBinning       → 最优分箱+WOE转换
4. 单调性检验            → 确保业务可解释
5. 相关性过滤            → 删冗余变量（200→80）
6. LightGBM特征重要性   → 精筛核心变量（80→30）
7. Logistic Regression  → 最终评分卡建模
8. KS/AUC评估          → 模型效果验证
9. 规则提取             → 树模型→IF-THEN策略规则
10. PSI监控             → 上线后持续监控稳定性
```

**为什么这个顺序？**
- 先粗筛再精筛 → 节省计算资源
- IV在前、LGBM在后 → IV是无监督统计量（快），LGBM是有监督模型（慢）
- 单调性在建模前检验 → 不单调的变量进LR会导致系数为负（不可解释）
- PSI最后 → 上线后持续做，不是一次性的

**追问防守：** "如果跳过IV直接用LGBM筛可以吗？"
→ 可以但不建议。IV能提前发现信息泄漏（IV>0.5）和无效变量，减少LGBM训练时间，也是一层sanity check。

---

## 万能应对公式

面试官问你"为什么用X方法"时，用这个结构回答：

> **"我们选X是因为三个原因：①业务需求（可解释/监管要求），②数据特点（样本量/特征类型），③实践验证（线上效果证明有效）。"**

举例："为什么最终用LR不用LGBM？"
> "三个原因：①监管要求评分卡可解释，LR系数可以直接对应评分；②我们的目标不是追求极致AUC，而是稳定+可落地；③实践中LR上线后PSI更稳定，LGBM容易随数据漂移。"

---

*最后更新：2026-08 | 配合代码实操效果更佳*
